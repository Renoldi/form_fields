# AppButtonGroup

Layout helper to group related action buttons using horizontal wrap or vertical stack.

## Basic Usage

```dart
AppButtonGroup(
  children: [
    AppButton(text: '4 oz', onPressed: () {}),
    AppButton(text: '8 oz', onPressed: () {}),
    AppButton(text: '12 oz', onPressed: () {}),
  ],
)
```
