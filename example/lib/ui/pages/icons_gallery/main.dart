import 'package:flutter/material.dart';
import 'view.dart';

class IconsGalleryPresenter extends StatelessWidget {
  const IconsGalleryPresenter({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const IconsGalleryPage();
  }
}
