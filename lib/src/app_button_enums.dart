library;

/// Defines the visual variant of [AppButton].
enum AppButtonType {
  filled,
  filledTonal,
  elevated,
  outlined,
  text,
  icon,
  fab,
  extendedFab,
}

/// Defines size presets for [AppButton].
enum AppButtonSize {
  small,
  medium,
  large,
  custom,
}
