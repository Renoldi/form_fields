library;

/// Defines the visual variant of [AppButton].
enum AppButtonType {
  filled,
  elevated,
  outlined,
  text,
  icon,
}

/// Defines size presets for [AppButton].
enum AppButtonSize {
  small,
  medium,
  large,
  custom,
}
